# SafeCall React Native client

Expo/React Native client for the Gemini Live demo. It fetches a one-use ephemeral token from the SafeCall backend and opens the constrained Gemini Live WebSocket directly from the device. It never receives the server's Gemini API key.

See the [workspace README](../README.md) for environment variables, emulator URLs, security configuration, and run commands.
